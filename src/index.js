// @flow

import React from 'react';

import { View, Text } from 'react-native';

const App = (): Object => (
  <View
    style={{
      width: '100%',
      height: '100%',
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: '#f0f',
    }}
  >
    <Text>BJS</Text>
  </View>
);

export default App;
